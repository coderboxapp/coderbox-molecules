import styled from 'styled-components'

export const DateRange = styled.div`
  display: flex;
  align-items: center;

  & .icon.arrow {
    margin: 0 0.3rem;
  }

  & .icon.calendar {
    width: auto;
    margin-right: 0.3rem;
  }
`
